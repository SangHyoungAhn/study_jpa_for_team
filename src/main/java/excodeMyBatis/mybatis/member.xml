<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="excodeMyBatis.mapper.MemberMapper">
    <insert id="save" parameterType="excodeMyBatis.model.Member">
        INSERT INTO MEMBER(NAME, AGE, ADDRESS, TEAM_ID)
        VALUES (#{name}, #{age}, #{address}, #{team.id})
    </insert>

    <update id="update" parameterType="excodeMyBatis.model.Member">
        UPDATE MEMBER
        SET NAME = #{name},
            AGE  = #{age},
            ADDRESS = #{address}
        WHERE ID = #{id}
    </update>


</mapper>