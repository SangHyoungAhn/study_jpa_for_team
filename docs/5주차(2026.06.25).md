# 5주차 (2026.06.25)

- [5주차 (2026.06.25)](#5주차-20260625)
    - [목표](#목표)
    - [0단계. 복습 — 4주차의 그 함정 (LazyInitializationException)](#0단계-복습--4주차의-그-함정-lazyinitializationexception)
    - [1단계. 연관관계 매핑 — 양방향과 연관관계의 주인](#1단계-연관관계-매핑--양방향과-연관관계의-주인)
        - [단방향 vs 양방향](#단방향-vs-양방향)
        - [연관관계의 주인 — mappedBy](#연관관계의-주인--mappedby)
        - [연관관계 편의 메서드](#연관관계-편의-메서드)
    - [2단계. N+1 문제 — LAZY의 그림자](#2단계-n1-문제--lazy의-그림자)
        - [왜 N+1이 생기나](#왜-n1이-생기나)
        - [⚠️ EAGER는 답이 아니다](#️-eager는-답이-아니다)
    - [3단계. Fetch Join — 한 방에 같이 가져오기](#3단계-fetch-join--한-방에-같이-가져오기)
        - [JOIN FETCH 문법](#join-fetch-문법)
        - [⚠️ Fetch Join의 함정 세 가지](#️-fetch-join의-함정-세-가지)
    - [4단계. @EntityGraph — JPQL 없이 선언으로](#4단계-entitygraph--jpql-없이-선언으로)
        - [언제 Fetch Join, 언제 @EntityGraph](#언제-fetch-join-언제-entitygraph)
    - [5단계. DTO 프로젝션 — 애초에 엔티티를 안 만든다](#5단계-dto-프로젝션--애초에-엔티티를-안-만든다)
        - [⚠️ 트레이드오프 — DTO는 변경 감지가 안 된다](#️-트레이드오프--dto는-변경-감지가-안-된다)
    - [6단계. OSIV — 편함과 위험의 트레이드오프](#6단계-osiv--편함과-위험의-트레이드오프)
        - [OSIV란?](#osiv란)
        - [켜기 vs 끄기](#켜기-vs-끄기)
    - [7단계. 선택 기준 — 언제 무엇을](#7단계-선택-기준--언제-무엇을)
    - [한 줄 요약](#한-줄-요약)

---

## 목표

> [!Important]
> 4주차에서 **"LAZY 프록시는 트랜잭션이 살아있어야 값을 채운다 → 닫히면 터진다"** 까지 봤다.
> 이번 주는 그 **해결책**을 배우되, 외우지 말고 아래 "왜?" 에 **원리로** 답하는 게 목표다.
>
> - 왜 양방향 매핑에는 "주인" 이 필요한가? — *1단계*
> - 왜 LAZY 인데도 쿼리가 N번 더 나가는가? — *2단계*
> - 왜 Fetch Join 이 가장 기본 해결책인가? — *3단계*
> - 언제 Fetch Join, 언제 `@EntityGraph` 인가? — *4단계*
> - 왜 어떤 조회는 엔티티가 아니라 DTO 로 가져오나? — *5단계*
> - OSIV 는 왜 켜면 편한데 위험한가? — *6단계*

> [!Note]
> 5주차는 "Repository 기초" 가 아니라 **연관관계 + 조회 최적화** 로 넘어가는 주차다.
> Fetch Join·`@EntityGraph`·DTO 는 Repository 에 *작성* 하지만, 배우는 개념은 **조회 전략** 이고,
> OSIV 는 아예 **웹/설정 레이어** 의 이야기다.

---

## 0단계. 복습 — 4주차의 그 함정 (LazyInitializationException)

- 프록시는 **통로(트랜잭션)** 가 살아있어야 값을 채운다 → 닫히면 터진다. (4주차 6단계 회수)
- 이번 주는 그 통로 문제를 푸는 **4가지 방법** 을 본다: Fetch Join / `@EntityGraph` / DTO / OSIV.
- 그런데 그 전에, N+1을 보여줄 **무대(연관관계)** 부터 깔아야 한다. → 1단계.

> [!Note]
> **준영속 상태에서 생기는 또 다른 함정 — merge 함정 (4주차에서 예고한 내용)**
>
> 트랜잭션이 끝난 뒤 반환된 엔티티(준영속)에 `save()`를 호출하면, 내부에서 `merge()`가 실행된다.
> `merge()`는 원본을 영속화하는 게 아니라 **복사본을 새로 만들어 반환**하므로,
> 반환값을 버리고 원본을 수정하면 변경 감지가 동작하지 않는다.
>
> ```java
> Member detached = memberService.findMember(1L); // 트랜잭션 밖 → 준영속
>
> memberRepository.save(detached);   // ❌ 반환값 버림 — 원본은 여전히 준영속
> detached.changeName("새이름");      // 변경 감지 안 됨 → DB 반영 X
>
> Member managed = memberRepository.save(detached); // ✅ 반환값을 받아야
> managed.changeName("새이름");       // managed가 영속 상태 → DB 반영 O
> ```
>
> **핵심:** `save()`의 반환값을 항상 받아서 사용할 것. 준영속 엔티티에 `save()`를 쓸 때 원본을 계속 쓰는 건 함정이다.

---

## 1단계. 연관관계 매핑 — 양방향과 연관관계의 주인

> 예시 엔티티: `Department 1 : N Member` (또는 게시판 `Post 1 : N Comment`)

### 단방향 vs 양방향

- 단방향: `Member → Department` 한쪽만 참조 (4주차까지 쓴 형태)
- 양방향: `Department → List<Member>` 까지 추가해 양쪽에서 탐색
- **왜 양방향이 필요한가** — "부서에서 소속 회원 목록" 같은 반대 방향 탐색이 필요할 때만. (필요 없으면 단방향이 낫다)

### 연관관계의 주인 — mappedBy

- 외래키(FK)는 **한 곳**(N쪽, `Member.dept`)에만 있다.
- `mappedBy` 로 "나는 주인이 아니라 거울일 뿐" 을 표시 → 주인 = **FK 를 가진 쪽**.
- ⚠️ **왜 주인이 필요한가** — 양쪽이 동시에 FK 를 제어하면 어느 쪽 값으로 UPDATE 할지 충돌. 주인을 정해 쓰기 책임을 하나로.

### 연관관계 편의 메서드

- 양방향이면 양쪽 객체를 함께 맞춰줘야 메모리와 DB 가 어긋나지 않는다.
- `addMember()` 같은 편의 메서드로 양쪽 세팅을 한 번에. (한쪽만 set 하면 생기는 버그)

---

## 2단계. N+1 문제 — LAZY의 그림자

### 왜 N+1이 생기나

- 목록 조회 1번(1) + 각 row 의 연관 엔티티 접근마다 추가 쿼리(N) = **1 + N**.
- `show-sql` 로 **N번 찍히는 걸 직접 눈으로** 확인. (4주차의 hands-on 톤 유지)

### ⚠️ EAGER는 답이 아니다

- 학생 본능: "그럼 `fetch = EAGER` 로 바꾸면 되잖아?"
- ⚠️ EAGER 는 **더 나쁘다** — 항상·모든 곳에서 조인이 끌려와 통제 불가, 페이징·성능 문제.
- 결론: **기본은 LAZY 로 두고, 필요할 때만 당겨오는** 게 정석 → 3단계.

---

## 3단계. Fetch Join — 한 방에 같이 가져오기

### JOIN FETCH 문법

- `SELECT m FROM Member m JOIN FETCH m.dept` — 연관 엔티티를 **조인해서 한 번에**.
- N+1 → **쿼리 1번** 으로 줄어드는 원리.

### ⚠️ Fetch Join의 함정 세 가지

- **페이징** — 컬렉션(`1:N`) fetch join + 페이징은 위험(메모리 페이징으로 빠짐).
- **중복 row** — `1:N` 조인은 행이 뻥튀기됨 → `DISTINCT` 필요.
- **컬렉션은 하나만** — 둘 이상 컬렉션 fetch join 금지(카테시안 곱).

---

## 4단계. @EntityGraph — JPQL 없이 선언으로

- 메서드 위에 `@EntityGraph(attributePaths = "dept")` 만 붙이면 Fetch Join 과 같은 효과.
- 쿼리 문자열을 안 써도 됨 → 기존 쿼리 메서드/`findAll` 에도 적용 가능.

### 언제 Fetch Join, 언제 @EntityGraph

- **판단 기준 표** (가독성·재사용·쿼리 복잡도 기준)
- 대략: 단순히 "같이 가져오기" 만이면 `@EntityGraph`, 조건·정렬이 복잡하면 Fetch Join(JPQL).

---

## 5단계. DTO 프로젝션 — 애초에 엔티티를 안 만든다

- 필요한 컬럼만 `SELECT new ...Dto(m.name, m.dept.name)` → **프록시·N+1 자체가 안 생김**.
- 조회 전용 화면(목록/리포트)엔 DTO 가 정석.

### ⚠️ 트레이드오프 — DTO는 변경 감지가 안 된다

- DTO 는 영속 객체가 아니므로 **읽기 전용**. 값 바꿔도 UPDATE 안 나감(당연).
- 그래서 **"읽기만 할 화면 → DTO", "수정도 할 흐름 → 엔티티 + fetch join"** 으로 갈린다.

---

## 6단계. OSIV — 편함과 위험의 트레이드오프

### OSIV란?

- Open-Session-In-View = 요청이 끝날 때까지 **영속성 컨텍스트를 살려둠**.
- 그래서 컨트롤러/뷰에서 LAZY 를 건드려도 안 터진다. (4주차 함정이 "안 생기는" 이유)

### 켜기 vs 끄기

- 켜기(스프링 기본값): 편하다. 하지만 ⚠️ **DB 커넥션을 요청 내내 붙잡아** 성능·확장성 발목.
- 끄기: 커넥션을 빨리 반납. 대신 LAZY 를 **트랜잭션 안에서 미리 처리**(fetch join/DTO)해야 함.
- 실무 권장: **OSIV off + 명시적 조회(fetch join / DTO)**.

> [!Caution]
> OSIV를 끄면 서비스 트랜잭션이 끝나는 순간 반환된 엔티티가 **준영속**이 된다.
> 이 엔티티를 컨트롤러에서 다시 `save()`하면 0단계에서 본 **merge 함정**이 그대로 터진다.
> OSIV off를 선택했다면 **반환값을 DTO로 변환하거나, save() 반환값을 반드시 사용**해야 한다.

---

## 7단계. 선택 기준 — 언제 무엇을

| 상황 | 권장 | 이유 |
| --- | --- | --- |
| 단건 + 연관 몇 개, 수정도 함 | Fetch Join / `@EntityGraph` | 엔티티 유지 → 변경 감지 가능 |
| 목록·리포트 (읽기 전용) | DTO 프로젝션 | 프록시·N+1 원천 차단 |
| 전역 LAZY 편의 | (지양) OSIV on | 편하지만 커넥션 비용 |

---

## 한 줄 요약

> N+1 은 **LAZY 의 그림자** 다.
> 트랜잭션 안에서 미리 당겨오거나(**Fetch Join · `@EntityGraph`**),
> 애초에 엔티티를 안 만들거나(**DTO**),
> 통로를 늘리거나(**OSIV**) — **각각 대가가 다르다.**
> 정공법은 LAZY 를 기본으로 두고 **필요할 때만 명시적으로 당겨오는** 것.
